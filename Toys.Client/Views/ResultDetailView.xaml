<Window x:Class="Toys.Client.Views.ResultDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Toys.Client.Views"
        mc:Ignorable="d"  
        Icon="../favicon.ico" Title="Detail" WindowStartupLocation="Manual" ResizeMode="NoResize" 
        Width="800" SizeToContent="Height" MaxHeight="494.4">

    <Grid Margin="16 8 16 16">
        <StackPanel Orientation="Vertical"  HorizontalAlignment="Center">
            <RichTextBox FontSize="16" IsReadOnly="True" VerticalContentAlignment="Center">
                <FlowDocument>
                    <Paragraph Margin="2">
                        <Run Text="{Binding Src}"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>

            <RichTextBox x:Name="ResultBox" Margin="0 8 0 0" FontSize="16" VerticalContentAlignment="Center">
                <FlowDocument>
                    <Paragraph Margin="2">
                        <Run Text="{Binding Dst}"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>

        </StackPanel>
    </Grid>
</Window>
